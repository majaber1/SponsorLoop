import { CreateOpportunityForm } from "@/components/create-opportunity-form";import{isLocale}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;const ar=locale==="ar";return <section className="page-section"><div className="container narrow"><div className="page-title"><span className="eyebrow">Inventory</span><h1>{ar?"حوّل جمهورك أو مساحتك إلى فرصة قابلة للبيع":"Turn your audience or asset into sellable inventory"}</h1><p>{ar?"أضف السعر والوصول والصيغ المتاحة. تظهر حالة التحقق بوضوح للمشترين.":"Add pricing, reach and available formats. Buyers always see the verification state."}</p></div><CreateOpportunityForm locale={locale}/></div></section>}
