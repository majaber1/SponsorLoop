import { CreateCampaignForm } from "@/components/create-campaign-form";import{isLocale}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;const ar=locale==="ar";return <section className="page-section"><div className="container narrow"><div className="page-title"><span className="eyebrow">Campaign Brief</span><h1>{ar?"أنشئ حملة في دقائق":"Create a campaign in minutes"}</h1><p>{ar?"أدخل فقط المعلومات التي تغيّر قرار المطابقة. يمكنك إضافة الموافقات والتواريخ والتفاصيل لاحقًا.":"Start with the inputs that affect matching. Approvals, dates and deeper details can be added later."}</p></div><CreateCampaignForm locale={locale}/></div></section>}
