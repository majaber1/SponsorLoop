import Link from "next/link";import{AuthForm}from"@/components/auth-form";import{isLocale,localePath}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;const ar=locale==="ar";return <section className="auth-section"><div className="container auth-layout"><div className="auth-copy"><span className="eyebrow">Create Workspace</span><h2>{ar?"ابدأ كعلامة تجارية، مالك حقوق أو وكالة.":"Start as a brand, rights holder or agency."}</h2><p>{ar?"نفس الحساب يمكنه لاحقًا إدارة أعضاء وصلاحيات وموافقات داخل المنشأة.":"The same workspace can later support team members, roles and approvals."}</p></div><div><AuthForm locale={locale} mode="sign-up"/><p className="auth-switch">{ar?"لديك حساب؟":"Already registered?"} <Link href={localePath(locale,"auth/sign-in")}>{ar?"دخول":"Sign in"}</Link></p></div></div></section>}
