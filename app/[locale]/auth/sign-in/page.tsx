import Link from "next/link";import{AuthForm}from"@/components/auth-form";import{isLocale,localePath}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))return null;const ar=locale==="ar";return <section className="auth-section"><div className="container auth-layout"><div className="auth-copy"><span className="eyebrow">SponsorLoop Workspace</span><h2>{ar?"كل الرعايات والفرص والصفقات في مساحة عمل واحدة.":"One workspace for campaigns, inventory and deals."}</h2><p>{ar?"ابدأ بحساب تجريبي أو أنشئ منشأتك. عند ربط PostgreSQL تصبح الحسابات والعضويات دائمة.":"Use the demo account or create your organization. With PostgreSQL connected, accounts and memberships become durable."}</p></div><div><AuthForm locale={locale} mode="sign-in"/><p className="auth-switch">{ar?"ليس لديك حساب؟":"No account yet?"} <Link href={localePath(locale,"auth/sign-up")}>{ar?"إنشاء حساب":"Create one"}</Link></p></div></div></section>}
