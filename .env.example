# Leave DATABASE_URL empty for built-in demo mode.
DATABASE_URL=
SESSION_SECRET=replace-with-at-least-32-random-characters

# Optional OpenAI-compatible /chat/completions endpoint for explanation reranking.
AI_PROVIDER_URL=
AI_PROVIDER_API_KEY=
AI_MODEL=

# Optional production integrations. V2 exposes the integration boundaries but does not fake external success.
OBJECT_STORAGE_URL=
OBJECT_STORAGE_TOKEN=
ESIGN_PROVIDER_URL=
ESIGN_PROVIDER_TOKEN=
PAYMENT_PROVIDER_URL=
PAYMENT_PROVIDER_TOKEN=
